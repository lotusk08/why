<div class="hint" aria-hidden="true">
  <em>Double-click anywhere to add a premise.</em>
  <span>Drag one idea onto another to link them.</span>
</div>

<style>
  .hint {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.35rem;
    padding: 1rem;
    text-align: center;
    color: var(--text-muted-color);
    pointer-events: none;
    animation: slide-enter 1s backwards;
  }

  .hint em {
    font-size: 1.45rem;
    color: var(--heading-color);
  }

  .hint span {
    font-size: 0.9rem;
  }
</style>
